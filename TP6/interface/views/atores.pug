extends layout
block content
  h2 Lista de Atores
  table.w3-table-all.w3-hoverable
    tr.w3-indigo
      th ID
      th Nome
      th Nº Filmes
    each a in atores
      tr
        td: a(href=`/atores/${a._id}`) #{a._id}
        td= a.nome
        td= a.filmes.length