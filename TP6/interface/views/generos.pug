extends layout
block content
  h2 Lista de Géneros
  table.w3-table-all.w3-hoverable
    tr.w3-indigo
      th ID
      th Designação
      th Nº Filmes
    each g in generos
      tr
        td #{g._id}
        td= g.designacao
        td= g.filmes.length